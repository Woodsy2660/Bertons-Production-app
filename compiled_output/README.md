# Compiled PDFs

Runtime output for manager-compiled run packs.

- File contents are **gitignored** (not part of the source build).
- Configured via `COMPILED_OUTPUT_DIR` (default `./compiled_output`).
