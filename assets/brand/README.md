# Brand assets

Official Berton logo package (source files). The **runtime app** uses copies under `app/static/img/` (especially `berton_logo.png` for PDF branding).

Do not put production secrets here. These files are design/source assets, not required at container runtime if `app/static/img` is present.
