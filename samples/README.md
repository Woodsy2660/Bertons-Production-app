# Sample documents

Non-runtime samples for manual testing and documentation.

| Path | Purpose |
|------|---------|
| `work_orders/` | Example work-order PDFs (also mirrored in `tests/fixtures/work_orders` for pytest) |

Runtime uploads/compiles go under `uploads/` and `compiled_output/` (gitignored).
