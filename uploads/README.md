# Uploads

Runtime storage for work orders, EzyWine listings, and label reference PDFs.

- File contents are **gitignored** (not part of the source build).
- Configured via `UPLOAD_DIR` (default `./uploads`).
